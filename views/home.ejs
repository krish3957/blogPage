<%- include("header") %>
    <div class="box">
        <% for(var i=0 ; i<blogTitle.length ; i++){ %>
            <div class="item">
                <h2>
                    <%= blogTitle[i] %>
                </h2>
                <p>
                    <%= blogContnet[i] %>
                </p>
            </div>
            <% } %>
    </div>
<%- include("footer") %>    